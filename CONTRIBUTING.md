# 貢献ガイドライン

## Contributor License Agreement (CLA)

本プロジェクトへの初めてのプルリクエスト時に、[CLA（Contributor License Agreement）](CLA.md) への同意が求められます。

CLA は、あなたの著作権を保持したまま、プロジェクトにコントリビューションの使用権をライセンス付与する合意です。CLA Assistant Bot が PR 上で自動的に同意を求めますので、指示に従って署名してください。

## ルール追加のルール

新しい仕訳ルールを追加する場合、以下を守ってください:

1. **根拠URL必須**: 国税庁・公式ドキュメントのURLを必ず記載
2. **JSON形式**: `rules/journal-rules.json` の既存フォーマットに従う
3. **テスト追加**: `tests/rules.test.ts` にテストケースを追加

## ルールのフォーマット

```json
{
  "id": "rule-29",
  "name": "ルール名",
  "patterns": ["キーワード1", "キーワード2"],
  "matchType": "partial",
  "accountName": "勘定科目名",
  "taxCategory": "課税仕入10%",
  "confidence": 0.90,
  "sourceUrl": "https://...",
  "notes": "補足事項"
}
```

## 根拠のないルールは受け付けません

「一般的にこう処理する」だけでは不十分です。必ず公式ドキュメントのURLを添えてください。

## ルール追加の判断基準

新しいルールをマージする前に、以下の5項目を必ず確認してください。

### ① 汎用性チェック

特定1社・1サービス固有の処理ではなく、**業界共通**（同業種・同取引類型で広く発生する仕訳）であることを確認する。
個社限定の特殊処理はルール化せず、利用者側で個別対応してください。

### ② 衝突チェック

新しい `patterns` の各要素が既存ルールの `patterns` と部分文字列（substring）として衝突しないかを確認する。
衝突が疑われる場合は `excludePatterns` の追加、またはルール順序の調整を検討してください。

### ③ confidence ルーブリック

| 値 | 基準 |
|---|---|
| `0.95` | 公式科目一覧（国税庁・e-Tax等）で明確に科目が指定されている |
| `0.90` | 実務上ほぼ確実（多数の税理士事務所・公式解説で一貫した処理） |
| `0.80` | 文脈によって勘定科目が変わりうる（要文脈判断・補足説明推奨） |

`0.80` 未満のルールは原則マージしないでください。

### ④ 根拠URL必須

`sourceUrl` に国税庁・e-Tax・公式ドキュメントの**直接リンク**を記載してください（既存ルールと同様）。
URLが存在しない場合・到達不能な場合はマージ不可です。

### ⑤ priority フィールド（衝突解決）

`priority` は整数値で、**値が大きいほど優先順位が高い**（デフォルト未設定は `0` 扱い）。複数ルールが同一テキストにマッチする衝突が起きる場合のみ設定し、より具体的・狭いパターンのルールに正の値（例: `1`）、より汎用・広いパターンのルールに負の値（例: `-1`）を付与してください。

### ⑥ 多義ベンダーの excludePatterns 検討

ベンダー名が複数の取引区分（例: ガス会社が「家庭用ガス」と「事業用ガス」を兼ねる）にまたがる場合、
または一般名詞と混同されやすいパターンの場合は `excludePatterns` に除外キーワードを追加してください。

---

## インボイス制度・経過措置フィールド

### invoiceRequired / transitionalDeductionRate

免税事業者等からの課税仕入に関して、インボイス制度の対応が必要なルールには以下のフィールドを付与します。

| フィールド | 型 | 意味 |
|---|---|---|
| `invoiceRequired` | `boolean` | `true` のとき、仕入先のインボイス登録番号確認が必要 |
| `transitionalDeductionRate` | `number \| null` | 現時点の経過措置控除割合。`0.8`（80%）/ `0.5`（50%）/ `null`（控除不可） |

### 免税事業者からの課税仕入の経過措置（消費税法附則第52条・第53条）

- 2023-10-01 〜 2026-09-30: **80%控除**（`transitionalDeductionRate: 0.8`）
- 2026-10-01 〜 2029-09-30: **50%控除**（`transitionalDeductionRate: 0.5`）
- 2029-10-01 以降: **控除不可**（`transitionalDeductionRate: null`）

`transitionalDeductionRate` は「現在（ルール更新時点）の控除割合」を示します。経過措置期間が変わるたびに値を更新してください。

---

## バージョニング（SemVer）

本リポジトリは [Semantic Versioning 2.0.0](https://github.com/zeimu-ai/.github/blob/main/VERSIONING.md) に準拠しています。
パターン（`patterns`）の変更は利用側の仕訳結果に直接影響するため、変更種別ごとに以下のバージョンを上げます。

| 変更種別 | バージョン | 例 |
|---|---|---|
| `patterns` の追加・変更・削除（既存の仕訳結果が変わる） | **MINOR** | マッチキーワードの追加、パターンの絞り込み |
| `taxCategory` の意味変更・正規化変更・スキーマの破壊的変更 | **MAJOR** | 税区分の定義変更、フィールド名の変更 |
| 純粋な追加（既存ルールを変更しない新規ルール追加）・誤字修正 | **PATCH** | 新規ルールID追加、`notes` の誤字修正 |

> **0.x 系（現在）の扱い**: 本データセットは現在 0.x 系（初期開発・API は変わりうる）です。SemVer 慣習に従い、0.x では破壊的変更も **MINOR 繰り上げ**（例: 0.3→0.4）で表します。上表の「意味変更＝MAJOR」の厳格運用は **v1.0.0 到達後**から適用します。

> **なぜ `patterns` 変更が MINOR か**: ルールエンジンの利用者は `patterns` にマッチした結果をもとに仕訳を自動生成します。パターンの変更は同一入力に対して異なる勘定科目・税区分を返す可能性があり、利用者側の動作変更を伴うためです。

---

## 税区分が文脈依存で割れる科目の扱い

摘要だけで税区分が一意に決まらない取引は、**一律分類せず**、対象を限定したうえで `notes` に区分を明記します。

- **例: 資格更新料** — (A) 行政・法定団体への登録更新／法定講習は租税公課・**非課税**（消費税法別表第一5号, 基通6-5-1）、(B) 民間資格（FP・簿記検定等）の更新手数料は支払手数料・**課税**（基通5-5-3）、(C) 同業者団体の通常会費は諸会費・**不課税**。摘要だけで判別できないため、諸会費ルール（不課税）の対象は税理士会・商工会等の団体会費に限定し、「資格更新」はパターンに含めません。
